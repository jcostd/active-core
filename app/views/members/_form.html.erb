<%= form_with(model: member, class: "space-y-4") do |f| %>
  <%= render "shared/form_errors", model: member %>

  <%# --- SEZIONE 1: DATI ANAGRAFICI --- %>
  <fieldset
    class="fieldset"
    data-controller="fiscal-code"
  >
    <legend class="fieldset-legend font-bold text-base flex items-center gap-2"><%= icon("badge", classes: "size-4") %>Dati Anagrafici</legend>

    <%# RIGA 1: Nome e Cognome %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 w-full">
      <div>
        <%= f.label :first_name, "Nome*", class: "label" %>
        <%= f.text_field :first_name, required: true, class: "input w-full", placeholder: "Mario" %>
      </div>

      <div>
        <%= f.label :last_name, "Cognome*", class: "label" %>
        <%= f.text_field :last_name, required: true, class: "input w-full", placeholder: "Rossi" %>
      </div>
    </div>

    <%# RIGA 2: Codice Fiscale e Data Nascita %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 w-full mt-2">
      <div>
        <%= f.label :fiscal_code, "Codice Fiscale*", class: "label" %>
        <%= f.text_field :fiscal_code,
	 required: true,
	 class: "input w-full uppercase font-mono tracking-wide",
	 placeholder: "RSSMRA...",
	 data: {
           fiscal_code_target: "code",
           action: "input->fiscal-code#autofill"
          } %>
      </div>

      <div>
        <%= f.label :birth_date, "Data di Nascita*", class: "label" %>
        <%= f.date_field :birth_date,
	 required: true,
	 class: "input w-full transition-colors duration-300",
	 data: { fiscal_code_target: "birthDate" } %>
      </div>
    </div>
  </fieldset>

  <%# --- SEZIONE 2: RESIDENZA --- %>
  <fieldset class="fieldset">
    <legend class="fieldset-legend font-bold text-base flex items-center gap-2"><%= icon("home", classes: "size-4") %>Residenza</legend>

    <%# Indirizzo Completo %>
    <div>
      <%= f.label :address, "Indirizzo", class: "label" %>
      <%= f.text_field :address, class: "input w-full", placeholder: "Via Roma, 10" %>
    </div>

    <%# Città e CAP %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 w-full mt-2">
      <div>
        <%= f.label :city, "Città", class: "label" %>
        <%= f.text_field :city, class: "input w-full", placeholder: "Milano" %>
      </div>

      <div>
        <%= f.label :zip_code, "CAP", class: "label" %>
        <%= f.text_field :zip_code, class: "input w-full font-mono", placeholder: "20100" %>
      </div>
    </div>
  </fieldset>

  <%# --- SEZIONE 3: CONTATTI E INFO MEDICHE --- %>
  <fieldset class="fieldset">
    <legend class="fieldset-legend font-bold text-base flex items-center gap-2"><%= icon("contact_mail", classes: "size-4") %>Contatti & Salute</legend>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 w-full">
      <div>
        <%= f.label :email_address, "Email", class: "label" %>
        <%= f.email_field :email_address, class: "input w-full", placeholder: "mario@esempio.com" %>
      </div>

      <div>
        <%= f.label :phone, "Telefono", class: "label" %>
        <%= f.telephone_field :phone, class: "input w-full", placeholder: "333 1234567" %>
      </div>
    </div>

    <%# Certificato Medico %>
    <fieldset class="fieldset">
      <%= f.label :medical_certificate_expiry, "Data Scadenza Certificato Medico", class: "label" %>
      <%= f.date_field :medical_certificate_expiry, class: "input w-full" %>

      <p class="label">
        <span class="label-text-alt text-base-content/60">Lasciare vuoto se non presente.</span>
      </p>
    </fieldset>
  </fieldset>

  <!-- Begin Actions -->
  <div class="modal-action">
    <%= f.submit "Salva",
     class: "btn btn-primary px-4",
     data: { turbo_submits_with: 'Salvataggio...' } %>
  </div>
  <!-- End Actions -->

<% end %>
