<%= turbo_frame_tag (turbo_frame_request_id || "modal") do %>

  <dialog
    class="modal modal-bottom sm:modal-middle"
    data-controller="dialog"
    data-action="close->dialog#cleanUp"
    data-turbo-cache="false"
  >

    <% box_classes = content_for?(:modal_class) ? yield(:modal_class) : "" %>
    <div class="modal-box <%= box_classes %>">

      <button
        type="button"
        class="btn btn-sm btn-square btn-ghost absolute right-2 top-2"
        data-action="click->dialog#close"
      >
        <%= icon("close", classes: "size-4") %>
      </button>

      <% if content_for?(:title) %>
        <h3 class="font-bold text-xl mb-4">
          <%= yield :title %>
        </h3>
      <% end %>

      <div class="mt-4">
        <%= yield %>
      </div>

    </div>

  </dialog>

<% end %>
